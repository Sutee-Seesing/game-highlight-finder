# VDO Current State

Updated: 2026-09-17 Asia/Bangkok

## Source B cached-video integrity finding (2026-09-17)

The historical Source B (`2026-08-24 23-42-23.mkv`) local-only proxy under `.t/c1-real-media-validation/data-b` is **not valid visual evidence for the full VOD**: ffprobe reads the MP4 container/audio at 4526.059s but video stream duration only 1545.267s (46,358 frames). Independent frame extraction from the raw source succeeded at 1800s and 3600s, so the raw source still has video beyond the cached proxy tail. The historical proxy metadata had accepted the full container duration without checking the video stream. Keep this old proxy and its derivatives as forensic evidence only; do not reuse it for visual/OCR review or claim that its video covers the full source. `validate_proxy_probe` now checks video-stream duration against the source independently (1s tolerance) both on newly generated proxies and on cache hits. Regression tests cover a full-length-container/short-video mismatch and rejection of a previously valid-hash cache with a short video stream. The historical B artifact was independently rejected by the production validator: `1545267 vs 4526017 ms`. Focused media regression passed 31/31 (1 CUDA-conditional skip), Ruff and mypy src (74 files) passed, and full-suite task `a76b4c21-e71a-47a3-84f9-e1ad9468e8ba` passed 329/329 (1 CUDA-conditional skip) in 121.95s. The isolated CUDA/local-only rerun task `79259e75-dde6-4efb-9eaf-076f0ee8313c` is now **COMPLETED exit 0** in distinct `data-b-gpu` with fake Scout and remote upload disabled. Fresh proxy video is 4526.066667s / 135,782 frames, proxy AAC is 4526.058813s, container is 4526.130667s, and standalone analysis AAC is 4526.058813s against source 4526.017s; 10-second tail video/audio decodes both exited 0. Fresh local signals are complete at 9,053/9,053 half-second bins with zero grid gaps, final end 4,526,017ms, 5,046 active bins and zero warnings. The old `data-b` artifacts remain forensic-only; `data-b-gpu` is the accepted local-only Source B media/signals baseline for future factual visual/OCR inspection.

## GPU proxy experiment (local-only)

**Full-application CUDA validation is green for provider-free Source C after bounded audio mixing.** The isolated CUDA full-source FFmpeg encode completed and ffprobe confirmed full video/audio plus successful 12-second tail decodes, but the separate full `highlight analyze` run with CUDA config (`c14c73e8-1565-4e43-a8fd-486f242bb3df`) failed *before video encoding*, when its four-track `amix -> pcm_f32le NUT` file silently ended at `3,204.052s` versus source `6,853.967s` (exit 2 from application validation). This is a recurrence of an upstream, apparently variable long-form mixing early-EOF symptom, not evidence of CUDA failure. The accepted CPU-path proxy/audio/local-signals artifacts remain untouched. Bounded Source C isolates succeeded: 2700-3600s NUT `899.999125s`, 3600-4500s NUT `899.988125s`; a stream-copy concat with explicit per-segment duration was `1799.988125s`, and inspected packets met exactly at `900.000000s` without timestamp overlap. Long-form multi-track mixing now uses verified 900-second NUT segments and explicit-duration concat before the unchanged limiter, with a double-NUT temporary disk reserve and proxy cache v3. Focused tests passed 36/36 with one conditional CUDA skip; explicitly enabled CUDA integration passed 1/1, Ruff and mypy src (74 files) passed. The isolated provider-free CUDA application rerun `957f7676-5788-4c4a-8f3d-f84b1c050465` **completed exit 0**: eight bounded NUT parts joined into full `pcm_f32le` mono 16k audio `6853.929813s` against source `6853.967s`; the CUDA proxy has H.264 video `6854.033333s` / AAC audio `6853.994813s` and standalone analysis AAC `6853.994813s`. Manifest stages ingest/proxy/local_signals are COMPLETED; activity has 13,708 bins reaching source end `6853967 ms`, warnings 0. Twelve-second tail video and analysis-audio decodes each exited 0. Original accepted CPU derivatives are intact, and no provider calls/uploads were made. This establishes full-length functional media, not a matched total-pipeline speed benchmark or proof of perfect seam acoustics.

A follow-up seam diagnostic decoded a 0.5-second window around each of the seven 900-second joins in the new analysis AAC and compared with four-track source mixes. The exact-boundary sample jump was never the largest jump in its inspected 0.5-second window; this is a limited signal check, not a claim that a listener would hear no click. The old *full-duration* single-pass NUT contains digital silence in an inspected 5399.5–5400.5s interval, whereas the new segmented AAC has measurable sound there (mean −25.5 dB; peak −11.1 dB), demonstrating why length-only checks missed missing audio content. A local, untracked listening pack with seven 6-second decoded PCM excerpts plus a source-mix reference at minute 90 is at `.t/c1-real-media-validation/seam-review/README.md`; human listening is not yet recorded.

H2.3 provider-free visual/OCR evidence ingestion now accepts source-SHA/duration-bound `VisualEvidenceFixture` observations through both `hybrid proposals --visual-evidence` and `hybrid run-fixture --visual-evidence`. Factual-only `VISUAL_STATE_CHANGE` / `OCR_STATE_CHANGE` observations retain their timestamps, provenance and optional hypotheses; source mismatch and unrelated modality fail closed. Contract/CLI/integration focused tests passed 9/9; broader hybrid/CLI/M6 passed 54/54; final full pytest rerun task `aba2f22b-b2db-4325-95ff-44645d4636e4` passed 327 with 1 conditional CUDA skip after metadata-description provenance hardening. Ruff src/tests, mypy src 74, handoff JSON, and git diff --check passed. No real non-FPS/social visual fixture has yet been gathered, and benchmark GT is not detector evidence: **no creator recall improvement is claimed for this adapter yet**.

Fresh Source C hybrid replay on the validated `data-c-gpu` session is now provider-free and persisted: `hybrid proposals` task `596388af-450a-4a5e-98de-923ee5f139df` created 896 AUDIO_ACTIVITY-only proposal neighborhoods from 10,761 weak factual anchors (~470.618/source-hour), with zero explicit event hypotheses and zero multi-source neighborhoods. A deliberately experimental `hybrid route --weak-sample-interval-seconds 120` run (task `f0ba3fdd-a665-48e8-a321-5d42ebd2f06d`) selected 58 SAMPLED_WEAK proposals (~30.464/source-hour) and retained 838 as DEFERRED_WEAK; MUST_INSPECT=0 and SUPPORTED=0. Selection covers every 15-minute source bin (8/7/8/8/7/7/8/5 selected), with first start=0 ms and last end=6,852,000 ms. These are navigation samples, not 896 detected highlights or 58 creator-ready candidates. Prior C=0 proposal evidence came from an older, incomplete/differently mixed local-signal artifact; do not infer that signal gaps alone explain the entire difference. Next: get a non-audio factual evidence source or creator-labeled examples before considering semantic-provider calls.

The provider-free Source C rerun is complete (ingest cache hit, proxy and local-signals completed). FFprobe confirms the fresh CPU-path proxy video `6854.033333s`, proxy audio `6853.994688s`, and analysis audio `6853.994688s`. This closes the original full-source early audio EOF for those derivatives. The separate full-suite failures were stale production pricing timestamps in offline fake-provider tests; their test-only synthetic pricing snapshots now allow lifecycle coverage without altering the live price catalog or fail-closed cost checks. Full pytest task `c6717d9c-bf81-487a-a5c7-731f5dabcd91` passed 324 tests with 1 CUDA-conditional skip (the explicit TREx CUDA integration separately passed 1/1). Live pricing verified on 2026-08-12 remains stale under the 30-day policy and needs official re-verification before any paid provider run.

Opt-in `media.proxy.video_acceleration: cuda` now selects CUDA decode -> `scale_cuda` -> NVENC while leaving the validated multi-track NUT/audio-limiter two-pass untouched; default `cpu` retains current behavior. Config fingerprint includes the new setting to prevent a CPU proxy cache hit when CUDA was requested. On TREx RTX 4070, identical Source C 90s video-only samples at 3600s took 10.74s (CUDA, ~8.72x) versus 13.12s (software decode/scale plus NVENC, ~7.05x). FFmpeg-reported process CPU user+sys time was ~2.5s versus ~158.3s, respectively; these are one-off FFmpeg measurements, not total application CPU benchmarks. Both 3s comparison clips probed as 854x480/yuv420p/90 frames. A 5-minute real Source C CUDA proxy using the existing full mixed NUT and unchanged limiter passed in 33.71s (~9.01x), with video 300.000s and AAC 300.064s. Media/config/M2 focused tests passed 34/34 and the explicit CUDA multi-track integration passed 1/1; Ruff, mypy src (74 files), and diff-check passed. The isolated full-C GPU artifact task `e74be9b7-ff43-464b-8b08-26cc15828033` completed with FFmpeg exit 0 in ~12m04s (reusing the existing full mixed NUT, so not an equal total-pipeline comparison). FFprobe confirmed 854x480/yuv420p, video 6854.033333s, AAC 6853.994688s, 205621 video frames; both 12-second tail decode checks exited 0. The first CUDA application run `c14c73e8-1565-4e43-a8fd-486f242bb3df` failed before CUDA at the upstream mixed NUT early EOF (3,204.052s). The segmented-mix CUDA application retry `957f7676-5788-4c4a-8f3d-f84b1c050465` completed exit 0 against isolated data-c-gpu; its proxy video/audio, standalone analysis audio, local-signals end timestamp and tail decodes passed as recorded above. No new provider call or upload was authorized.

## Source C local-signal coverage correction (2026-09-17)

Do **not** treat the previously accepted CPU `data-c` analysis as complete merely because its proxy and AAC durations reach the end. A read-only side-by-side check found `12,872` half-second audio-activity bins in that historical CPU session versus `13,708` expected for `6,853,967 ms`: `836` intervals are absent in `33` clusters. The fresh segmented-audio + CUDA `data-c-gpu` session contains **all 13,708 expected bins**, each with the exact expected start/end timestamps (0 mismatches), and labels `576` of the intervals missing from the old session as active. Use the fresh GPU session for provider-free signal evaluation; do not rescore ground truth from the incomplete old CPU signal artifact. This establishes signal coverage, not a subjective listening assessment or a definitive root-cause attribution for every absent historical bin.

## Product North Star

Game Highlight Finder is a **multi-game creator assistant** whose purpose is to help the owner build an audience from zero with short-form gaming content (TikTok first) and eventually turn that audience into livestream viewers.

It is not an FPS kill detector. Competitive-FPS kill cases are engineering diagnostics because they provide clear temporal truth, but they do not define product quality.

Primary creator loop:

`play/record -> creator candidate pack -> choose moments -> AI rough-edit drafts -> human final edit/approval -> publish -> learn from real audience performance -> repeat`

Canonical creator strategy:

`docs/10_CREATOR_GROWTH_AND_AI_EDITING_PLAN.md`

## What the product should output first

The first usable product milestone is **Creator Candidate Pack Beta**:

- analyze a long 1–4+ hour VOD;
- surface a small ranked set of creator-worthy moments;
- support multiple games/gameplay styles;
- treat funny/fail/reaction/friend/social/WTF/tension-payoff/discovery/personality/skill/clutch as first-class moment families;
- preserve setup -> event -> payoff context;
- extract candidate clips from the original source;
- let the owner review candidates instead of scrubbing the full VOD;
- keep detection confidence separate from creator/short-form worthiness.

The candidate clips are **raw material for editing**, not necessarily finished TikToks.

## AI editing direction

AI editing is now explicitly on the roadmap, but as a non-destructive **AI rough editor** after candidate discovery becomes usable.

Initial AI-edit scope should operate on owner-selected candidates and produce draft variants with:

- true-dead-air/pacing tightening, with the hard rule `silence != dead air` and `no speech != boring`;
- hook/opening suggestion;
- safe 9:16 reframing;
- draft captions/subtitles;
- loudness cleanup;
- restrained punch-ins/zooms;
- caption/on-screen-hook suggestions.

Always preserve the untouched candidate and keep human final approval. Fully automatic publishing remains later.

## C1 implementation progress

Provider-free implementation is now underway:

### 2026-09-11 local media correctness checkpoint

The multi-track OBS analysis WIP now includes long-VOD signal hardening without any provider activity. Local loudness analysis keeps the configured interval for normal recordings, but increases it only when needed to fit the complete duration inside the bounded 20,000-interval artifact contract (for example, a 4-hour source at the default 500 ms becomes 720 ms). FFmpeg sampling and loudness parsing use that same effective interval. The previous silent `[:20_000]` truncation paths were removed; pathological overflow now fails explicitly instead of silently losing the source tail. `LOCAL_SIGNALS_CACHE_VERSION` is now 2 so stale signal artifacts cannot be reused across this semantic change.

Validation for the long-VOD slice is green: focused provider-free regression **72/72 passed**, Ruff passed, `mypy src` passed across **74 source files**, `git diff --check` passed apart from existing line-ending warnings, and the full local suite passed **320/320 in 144.71s**. No provider call, upload, or new spend was made. The subsequent V-C1-C (`E:\\Obs\\2026-08-26 23-17-37.mkv`) provider-free rerun reached a separate execution-safety failure: proxy attempt `02c9cbdecf0c4b06b4ddc422da6717df` passed proxy-video validation, then failed analysis-audio duration validation. Direct source evidence shows all four AAC tracks report approximately `01:54:13.951`, packet timestamps continue across the observed cutoff and to the source tail, and single-track decoding continues beyond the cutoff. The exact production audio command without `-nostdin` then reproduced a graceful partial completion at `00:45:33.013` with exit code 0, empty stderr, and `progress=end`. The same production `amix + alimiter + AAC/M4A` graph with only `-nostdin` added crossed that boundary and completed its locked `-t 2800` target at exactly `00:46:40.000`; ffprobe reports `2800.021333` seconds. This identifies interactive FFmpeg stdin susceptibility as the supported failure mode rather than short source audio, padding, or a decoder error. All seven automated FFmpeg command builders are now hardened with `-nostdin`; the provisional silence-padding experiment remains withdrawn. Ruff is green, `mypy src` is green across 74 files, `git diff --check` is green apart from line-ending warnings, the post-hardening focused FFmpeg/media regression passed **51/51 in 18.85s** under task `776754a4-b3f6-4492-ba1c-cc3859a36c1f`, and the post-hardening full suite passed **320/320 in 155.86s** under durable task `019dee57-3878-411a-bf22-0ff71ff46a96`. The fresh provider-free C rerun under durable task `9451bc77-f79f-4bf8-bdb1-28e4369bcb13` / proxy attempt `01dc872a050b4a63bf0f57c6f711f4f4` also failed analysis-audio duration validation, now with explicit evidence `2,556,523 vs 6,853,967 ms` (~42:36.5 vs ~1:54:14). This proves `-nostdin` is valid execution hardening but is not the complete root cause of the full-source cutoff. `run_ffmpeg` itself has no matching hidden wall-clock limit (configured FFmpeg timeout remains 7,200s, and cancellation/nonzero exits would raise instead of being accepted). That full-source production diagnostic (`f7b41477-33c3-4175-92bd-e131ca2d9b5f`) also ended gracefully and incorrectly at `00:43:55.968`, exit 0, `progress=end`, with no warning output. Removing only `alimiter` then isolated the failure: full-source `amix` without the limiter completed at `01:54:13.930688`, and ffprobe measured `6853.994688s` against the `6853.967s` source. This rules out `amix`, AAC decoding, source EOF, muxing, wrapper timeout, and stdin as the remaining primary cause; the supported root-cause boundary is now the `alimiter` stage. A full-source fix-candidate with only `alimiter=limit=0.95:latency=1` (`ea4fb7d4-1390-4302-b98d-a439f34df752`) also failed gracefully at `00:43:33.930688`, exit 0, no stderr, so limiter latency compensation does not fix this source and must not be patched into production. The single-track limiter isolate from durable task `34b7077d-2ab3-4ea7-9a5c-b023858b29fb` is now proven full-duration: ffprobe reports `6853.994688s`, matching the `6853.967s` source within ~28 ms. This rules out an `alimiter`-intrinsic long-source failure and refines the failure boundary to the `amix(normalize=0) -> alimiter` interaction. The `asoftclip=type=hard:threshold=0.95` replacement diagnostic (`e8971389-9338-4aaa-9574-11fe8e8beefe`) also failed gracefully, ending at `00:38:21.802688`; ffprobe confirms only `2301.866688s`. Therefore merely replacing the lookahead limiter with another post-mix amplitude processor is not sufficient. The pass-through control task `6212ebf2-5127-46a4-9554-18a7a72fd057` is now full-duration: `amix=inputs=4:normalize=0:dropout_transition=0,anull` probes at `6853.994688s`, matching source within ~28 ms. Therefore generic post-`amix` chaining is not the issue; early EOF is specific to the amplitude/dynamics processors tried on this mixed stream (`alimiter` and `asoftclip`). The full-source `astats` attempt itself early-EOFed at `00:38:58.794667`, so it is not a trustworthy whole-source peak scan. A sequential <=20-minute pre-encode scan now covers the entire VOD instead: worst raw mix is `Max_level=1.754737` / `+5.198178 dBFS` in the 4800-6000s chunk, and every chunk reports `NaN=0` / `Inf=0`. The C-specific attenuation needed to target 0.95 would be about `0.541392` (`-5.32977 dB`), but that number must not be generalized to arbitrary track layouts. Resetting mixed audio timestamps with `asetpts=N/SR/TB` before `alimiter` was also rejected: ffprobe reports only `2300.224000s`. The `amix normalize=1` candidate task `010e5d74-3d9e-4c00-b98b-923087da72e8` is also rejected: ffprobe reports only `3490.944000s`. The first full-duration anti-clipping proof is now a file-boundary two-pass diagnostic: the proven full `amix normalize=0` artifact was fed into `alimiter=limit=0.95` in a second FFmpeg process and ffprobe reports `6853.994688s`. A decoded full-file scan reports `Max_level=1.052555` / `+0.444895 dBFS`, NaN=0, Inf=0. Lowering the limit to `0.90` also stayed full-duration but decoded peak increased to `1.041526` / `+0.689685 dBFS`, which led to inspection of local FFmpeg options: `alimiter` has `level` auto-level enabled by default. The second-pass `alimiter=limit=0.95:level=0` candidate is now also full-duration at `6853.994688s`; decoded scan improves to `Max_level=1.021416` / `+0.184052 dBFS`, NaN=0, Inf=0, so disabling auto-level is directionally correct but AAC decode still overshoots 0 dB slightly. The second-process `alimiter=limit=0.90:level=0` candidate is now accepted: ffprobe reports full `6853.994688s` and decoded full-file scan reports `Max_level=0.979226` / `Peak_level_dB=-0.182345`, NaN=0, Inf=0. This is the first candidate that is both full-duration and leaves decoded AAC headroom below 0 dBFS. The first exact lossless-intermediate proof was rejected: durable task `a978ede7-31d1-48c6-997e-82a2654686c8` used full source multi-track `amix normalize=0` -> `pcm_f32le` mono 16 kHz in WAV, but exited 0 at only `2975.914688s` (~49:35). This proves file-boundary separation alone is insufficient and that the intermediate format/container path participates in the early-EOF behavior. Production remains untouched. The NUT isolate task `7d4bc564-1971-4a6c-b443-b8f092c26e89` is now full-duration: ffprobe reports `6853.929625s` for `pcm_f32le` mono 16 kHz NUT, matching the source within ~37 ms. This proves the lossless float signal itself is viable and isolates the prior lossless-intermediate cutoff to the WAV/container path rather than PCM float. The exact NUT pass-2 task `3fb7a4c0-b381-4bec-9bb1-8db1e2040915` is full-duration (`6853.994688s`); decoded peak `0.972900` / `-0.073253 dBFS`, NaN=0 and Inf=0. The long-form multi-track production fix is now patched but uncommitted: mix into `pcm_f32le` NUT, limit the NUT audio separately for proxy and analysis AAC with `alimiter=limit=0.90:level=0`, preserve short extraction, bump proxy cache version to 2, and account for the NUT temp in multi-track disk preflight. Focused tests 26/26, Ruff and mypy src (74 files) pass. Full suite task `39425b74-6463-4443-9cb6-2b46a3766869` is not green: the first failure is a stale Gemini pricing fixture CostGateError; remaining failures are not yet fully classified, and no pricing guard was bypassed. Provider-free Source C rerun task `261b7dca-e26e-4d07-86f4-34bedf679ac2` is active through local-signals with fake Scout override; do not claim end-to-end media correction until its proxy audio, analysis audio, and local-signals tail pass.

- added generic `DISCOVERY` candidate category;
- added backward-compatible `moment_summary` (what happened) and `creator_reason` (why review it) candidate semantics;
- legacy Scout responses fall back to `reason` for those fields so old artifacts remain usable;
- added creator-balanced `gemini-scout-window-v20-creator` as the first creator prompt, then replaced the default with provider-free contract revision `gemini-scout-window-v21-editorial-role` after the A owner review exposed standalone-vs-montage and premature-payoff failures;
- the creator prompt explicitly treats visual-only gameplay as first-class and states `silence != dead air` / `no speech != boring`;
- social/personality moments can qualify without a gameplay payoff when they have a self-contained audience payoff;
- ranking now exposes an explicit `creator_score` while preserving short-form score and detection confidence;
- the report now renders a `Creator Candidate Pack` with `What happened`, `Why review this`, creator score, detection confidence, Open Clip, and technical lineage behind secondary details;
- `config.example.yaml` now uses the creator prompt default instead of the old v18 prompt;
- focused offline tests cover creator prompt semantics, `DISCOVERY`, creator fields, ranking, report rendering, quiet discovery, social payoff, fail/tension story, boring-zero-candidate behavior and legacy fallback;
- a provider-free end-to-end integration smoke test now exercises window payload -> canonicalization -> reconcile -> clip boundaries -> ranking -> extraction manifest -> report.

Focused C1 integration result: **4 passed in 0.56s**.

A local five-family Creator Candidate Pack demo was generated under `.t/c1-creator-pack-demo/` with synthetic playable MP4 fixtures/thumbnails for `DISCOVERY`, `FUNNY`, `FAIL`, `REACTION`, and `SKILL`. It uses the actual report/ranking pipeline and made **0 provider calls**.

The fresh post-H2.1 full local suite is green: **302 passed in 199.24s (0:03:19)** via durable task `3a7a4d49-f79d-4393-b8ce-2aeff0937750`. The source now includes deterministic proposal clustering, source-bound manual-marker enrichment, and CLI/session plumbing for that local evidence. Focused Hybrid/CLI/M6 regression is **40 passed in 56.44s**, Ruff is green, and mypy reports no issues in **72 source files**. This work remains provider-free.

Real-media validation is specified in `docs/13_C1_REAL_MEDIA_VALIDATION_PLAN.md`. V-C1-A fast-action live validation consumed the owner's one-shot authorization (**max 3 logical calls, hard cap ฿2.52, attempt 1, no auto-retry, analysis-window proxies only**). Task `fe945f6f-3a0c-4e06-a704-a4da17c807a9` completed with exactly **3 generation calls / 3 analysis-window uploads / 0 retries**, all three remote files recorded `deletion_status=deleted`. The ledger has exactly three `SETTLED` rows with aggregate actual cost **฿0.996318** and no reserved/in-flight ambiguity. Gemini surfaced **6 raw creator candidates**; provider-free finalize task `18357ac4-cc23-4e34-b395-19388a426250` then reconciled/extracted/ranked/reported all **6/6** with no incomplete extraction and no additional provider work.

Owner creator review is a **C1 product-quality fail for standalone usefulness: 0/6 are considered usable standalone posts**. The clearest semantic/boundary failure is a candidate that treated a Valorant Spike plant as a clutch/win-style payoff while the round was still unresolved with enemies remaining. Several other gunfight moments may still be usable as **montage beats**, which exposed the distinction between `real event` and `creator-usable story`.

The external Deep Research + targeted repo audit is now complete. Its decision is **CONTINUE + PIVOT + SIMPLIFY**: preserve the media/source/extraction/canonicalization/reconcile/cost/report substrate, but retire the monolithic long-window Scout as the semantic source of truth. The new canonical implementation plan is `docs/16_C1_HYBRID_CREATOR_TRIAGE_PLAN.md`: factual proposals -> proposal-centered context -> semantic judge -> dynamic context expansion -> factual resolution verifier -> story assembly/boundary verification -> creator ranking. v21 editorial roles remain useful but must be extended with independent story/resolution state and explicit evidence-backed claims. **B/C paid inference remains paused.** Creator review remains separate from GT-v2 event truth.

Hybrid implementation is now coherent through H6 plus provider-free proposal enrichment/routing. H1 adds `StoryState`, `ResolutionState`, evidence-backed `CandidateClaim`/`ClaimStatus`, and a ranking/report gate that prevents provisional/unverified `STANDALONE_STORY` items from entering normal creator review. H2 adds provider-neutral factual `Proposal` artifacts and a deliberately weak local activity adapter with no creator/editorial score. H2.1 adds source-bound `ManualProposalMarkerSet` fixtures; H2.2 adds source-bound `TranscriptFixture` / `ASR_UTTERANCE` evidence; H2.3 now adds source-bound `VisualEvidenceFixture` observations for `VISUAL_STATE_CHANGE` / `OCR_STATE_CHANGE`. The visual/OCR fixture is an ingestion contract for independently produced local evidence, not a detector or a way to replay benchmark GT as detector output. All source-bound fixtures feed the same deterministic clustering, persisted `ProposalSummary` density/duplication telemetry, and auditable routing states `MUST_INSPECT / SUPPORTED / SAMPLED_WEAK / DEFERRED_WEAK`. Routing never deletes factual evidence and does not assign creator value; its purpose is to bound expensive semantic inspection. Conflicting explicit event hypotheses are never merged, and source provenance/signal types remain visible. H3 adds bounded proposal-centered context planning and dynamic expansion. H4 adds a fake semantic judge whose outcome claims remain provisional. H5 adds an independent `CandidateVerification` / fake resolution verifier; verified or contradicted claims require timestamped evidence. H6 adds verified `StoryAssembly` boundaries for setup/event/payoff/reaction and refuses story assembly before `COMPLETE` + `VERIFIED/NOT_APPLICABLE`. `run_provider_free_hybrid_triage` joins those contracts end-to-end and persists full semantic/review maps under `session/hybrid/`; clip boundaries are derived only after the creator-review verification gate. Local CLI/session workflow now includes `hybrid proposals`, `hybrid route`, `hybrid run-fixture`, `hybrid review-template`, and `hybrid review-summary`; optional manual markers/transcript fixtures remain local. A separate creator-evaluation corpus records KEEP/MAYBE/rejection, owner role/boundary corrections, review time and `MISS_OBVIOUS` without contaminating GT. The historical Spike-plant failure is encoded as a provider-free replay regression: a semantic `ROUND_WON` hypothesis remains excluded from standalone review when the independent verifier has no terminal evidence. H3 now also materializes routed initial proposal-centered media under `session/hybrid/contexts/<context_id>/analysis_context.mp4`, cut only from the committed analysis proxy with deterministic context/materialization identity, parent-proxy provenance, hash validation, cache reuse, and RAW-source rejection at the future upload boundary; local CLI support is `hybrid contexts`. The new focused Hybrid/CLI/M6 regression is **49 passed in 133.74s**, Ruff is green across `src` + `tests`, mypy reports no issues in **73 source files**, and `git diff --check` is clean apart from line-ending normalization warnings. The fresh provider-free full suite for the routed context-media slice is green: **311 passed in 123.02s (0:02:03)** via task `47b33ae3-f340-4223-8301-95f151253094`. No provider calls/uploads were made by this work.

The next provider-boundary slice is also implemented provider-free. `hybrid provider-preflight <session>` now loads only committed routed `analysis_context.mp4` artifacts, validates their hashes/provenance against the current committed analysis proxy, requires exact one-context-per-routed-proposal coverage, builds a distinct `SEMANTIC_JUDGE` request contract, and quotes each logical request through the existing Gemini cost service without reserving funds, uploading media, or generating. Request plans persist prompt/schema hashes, provider request fingerprints, model-sensitive call IDs, cache keys, exact context duration, model-aware media/thinking semantics, and one-attempt/zero-auto-retry policy. Prompt contracts include explicit context-media -> source-relative timestamp mapping so a clipped context starting at media time zero cannot silently produce context-relative timestamps. The first-pass aggregate records logical-call count, media minutes, available budget and exact reserve; dynamic-context expansion is explicitly excluded until a separate re-preflight, and the independent `RESOLUTION_VERIFIER` preflight remains blocked until semantic output exists. The semantic contract treats all outcome claims as provisional; the verifier contract alone may verify/contradict terminal claims with timestamped evidence. Provider JSON-schema projections now derive their version/enums/field-set checks from the canonical `SemanticJudgment`, `CandidateVerification`, `CandidateClaim`, and `Evidence` contracts instead of duplicating semantic truth by hand. Focused provider-boundary regression is **22 passed in 0.90s**, Ruff is green across `src` + `tests`, `mypy src` is green across **74 source files**, `git diff --check` is clean apart from line-ending normalization warnings, and the fresh provider-free full suite is **314 passed in 145.51s (0:02:25)** via task `8afb5210-a28b-4efc-9b4e-6b27eddf743d`. RAW source upload remains forbidden and normal chat continuation does not authorize live work.

Provider-free proposal-density diagnostics now show why local activity cannot be the multi-game proposal backbone. Reusing A/cal-01 (600.886s) without reprocessing or provider I/O, weak local activity produced **860 factual audio anchors -> 84 clustered proposal neighborhoods**, equivalent to **503.26 neighborhoods/source-hour**, with no explicit hypotheses and no second evidence source. The new routing layer keeps all 84 neighborhoods auditable but can select deterministic weak-evidence coverage samples: **30s -> 20 selected (~119.8/hour)**, **60s -> 10 (~59.9/hour)**, **120s -> 5 (~30.0/hour)**. These are diagnostics only; no universal interval is locked. Reusing the existing B (~75m) and C (~114m) local-signal artifacts, the current factual adapter produced **0 proposal neighborhoods for B and 0 for C**. The A-too-dense/B-C-empty asymmetry is architecture evidence, not a threshold-tuning target: local audio/activity stays optional navigation evidence, while transcript/visual/OCR/game-event sources must carry recall where available.

V-C1-B is selected as `E:\\Obs\\2026-08-24 23-42-23.mkv` (75m26s quiet/exploration source). Provider-free ingest + proxy + local-signals completed successfully under session `2026-08-24_unknown_0db51591365f` via task `b801aa31-4d42-42da-901f-354273205cec`. The CLI safety guard has been narrowed so Gemini-backed M6 can execute the fully local `windows` stage without enabling remote upload, while Scout/reconcile/extract still require explicit upload authorization. Focused CLI tests pass **7/7**. Provider-free B window materialization completed via task `a5844d26-c3da-4633-8f88-d2dc7eced126`: **17/17 analysis-window proxies** are present. The v21 editorial-role contract was re-preflighted provider-free via task `d610af52-065d-48bf-88cb-4631151de7c9`: exact aggregate reserve **฿15.224148**, provider calls **0**, uploads **0**, new spend **฿0**. No B live inference is authorized.

V-C1-C is selected as `E:\\Obs\\2026-08-26 23-17-37.mkv` (114m14s social/Discord-heavy source). After the owner interrupted the first proxy attempt during CPU saturation, the clean retry completed provider-free: ingest reused its cache, proxy and local-signals completed via task `85b5f4fa-a8b2-4c12-8291-0d59d605d9fa`, then local window materialization completed via task `bd1e9980-7169-4e67-88cd-af3c4265412b` with **26/26 analysis-window proxies**. The v21 editorial-role contract was re-preflighted provider-free via task `e42a3019-3bb7-46ef-8e63-1921909971ae`: exact aggregate reserve **฿23.218504**, provider calls **0**, uploads **0**, new spend **฿0**. B/C remain local-only and unauthorized. C1 execution continues under the restricted file-scope rule while the Supervisor Cleanup/Scope fix is pending: **no mass rename, repo/worktree move, large-folder move, recursive cleanup, or new workspace structure**. Existing canonical project paths stay fixed. See `docs/14_PRE_REORG_CHECKPOINT_2026-09-07.md` for the pre-cleanup state and `docs/15_VDO_FOLDER_CLEANUP_2026-09-07.md` for the scoped cleanup record.

## Benchmark status / what we learned

GT v2 calibration truth remains preserved for regression work. Do not overwrite old GT or use revealed validation holdout for tuning.

Historical v19 300s-window calibration showed weak creator/event detection and missed both cal-01 MUST_CATCH kills.

v20 targeted 120s diagnostic:

- 2 paid calls, actual settled cost `฿0.453404`;
- recovered the `348–350s` kill;
- still missed the `447–448s` kill under the locked temporal ruler;
- false positives remained inside annotated boring intervals.

v21 targeted 90s diagnostic is complete:

- exactly 2 paid calls, no automatic retry;
- actual settled cost `฿0.423648`;
- first known kill surfaced at `346–353s` (GT `348–350s`);
- second known kill was semantically surfaced inside a broad `445–465s` multi-kill candidate (GT `447–448s`), showing detection improved but boundaries remained too broad for the locked IoU ruler;
- remote/provider lifecycle completed with no remaining reserved/in-flight/ambiguous exposure.

Interpretation: shorter context materially helps fast-event visibility, but this is enough evidence to stop treating window-length micro-optimization as the product goal. The next milestone is multi-game creator usefulness.

## Durable decisions

- Production quality is multi-game and creator-oriented.
- A kill is one highlight type, not the definition of highlight.
- Benchmarks are regression tools, not an endless shipping gate.
- `short_form_score` is an editorial prior, not a virality prediction.
- AI rough editing follows candidate discovery; human approval remains first-class.
- Real publish performance should later improve creator-specific ranking/editing without contaminating event-detection ground truth.

See `docs/DECISIONS.md` for durable rationale.

## Safety / workflow

- No new provider call is currently authorized.
- No automatic provider generation retries.
- Analysis proxy upload only when explicitly authorized; never raw source upload.
- Preserve existing benchmark artifacts and GT history.
- Do not clean/reset/stash/drop unrelated dirty work.

## Next action

See `docs/NEXT_ACTION.md`.
