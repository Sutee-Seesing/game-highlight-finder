# VDO Next Action

Updated: 2026-09-17 Asia/Bangkok

## Immediate Source B video-integrity gate

The historical `data-b` MP4 is **not full-length visual evidence**: ffprobe reports video 1545.267s / 46,358 frames while its audio/container reports 4526.059s; raw source frame extraction at 1800s and 3600s succeeded. Do not use the old proxy for full-VOD visual/OCR inspection or infer visual recall from it. Newly generated and cached proxies now require independently full-duration video streams (1s tolerance), with regression coverage for a full-length-audio/short-video file and a previously valid-hash cache. Preserve old artifacts for diagnosis; full suite is green (329 passed, 1 CUDA-conditional skip; task `a76b4c21-e71a-47a3-84f9-e1ad9468e8ba`) and the historical B proxy is rejected by the production validator. The gate is checkpointed at `5b5910c` and pushed. Isolated B CUDA/local-only rerun task `79259e75-dde6-4efb-9eaf-076f0ee8313c` **completed exit 0** in `data-b-gpu`: proxy video 4526.066667s / 135,782 frames, proxy audio 4526.058813s, standalone analysis audio 4526.058813s, both 10-second tail decodes exit 0, and local-signals coverage is 9,053/9,053 exact half-second bins ending at source 4,526,017ms with zero warnings. Use `data-b-gpu` rather than historic `data-b` for provider-free visual/OCR evidence. Keep observations source-bound and factual; do not replay GT as detector output. No paid provider call/upload is authorized.

First real B visual-routing measurement is complete provider-free: 52 diagnostic scene-change observations changed 607 audio-only neighborhoods / 0 multi-source into 613 hybrid neighborhoods / 43 multi-source. At the same experimental 120-second route interval, baseline selected 37 weak samples while hybrid selected 55 = 43 `SUPPORTED` + 12 `SAMPLED_WEAK`; both still cover all 37 temporal buckets. Treat this as routing-diversity evidence only, not creator-recall or creator-quality proof. Routed B proposal-centered context materialization is also complete: 55/55 local `analysis_context.mp4` clips generated under task `b4ab4bdd-789e-40a7-9138-c581ece3d387`, with RAW source upload forbidden and provider calls zero. A pre-review deterministic cohort is locked at `.t/c1-real-media-validation/source-b-routing-audit-cohort-v1.json` (12 `SUPPORTED`, all 12 `SAMPLED_WEAK`, 12 `DEFERRED_WEAK` controls). The blinded review pack is now materialized at `.t/c1-real-media-validation/source-b-routing-audit-review-v1/review.html`: 36/36 cases, 24 existing routed contexts reused, 12 deferred controls generated from the validated `data-b-gpu` proxy, 36 contact sheets, video+audio present for every clip, and maximum clip-duration drift versus the planned context is 64 ms. Reviewer-facing HTML/worksheet do not expose the three routing-group values; `truth_map.json` must stay unopened until review is complete. Next: complete the blinded owner review for creator usefulness/review burden and obvious misses, then unblind and compare `SUPPORTED` vs `SAMPLED_WEAK` vs `DEFERRED_WEAK`. Do not infer creator quality from scene-change support alone. Reuse `.t/c1-real-media-validation/c1-b-quiet-exploration-cuda-local.yaml` for this session so a config mismatch does not invalidate the accepted proxy cache.

## GPU proxy follow-up

The optional `media.proxy.video_acceleration: cuda` implementation passed the provider-free full-VOD validation; default remains `cpu`, and CUDA requires `h264_nvenc`. Source C 90-second video-only comparison measured CUDA wall 10.74s vs existing CPU decode/scale + NVENC wall 13.12s; FFmpeg process CPU time ~2.5s vs ~158.3s. The 5-minute real C GPU-video + full NUT-audio/limiter sample passed in 33.71s, with video 300.000s/audio 300.064s. Tests: 34/34 focused media/config/M2, 1/1 explicit CUDA multi-track integration, Ruff, mypy src 74 and diff-check passed. The isolated ignored full-C GPU output `.t/c1-real-media-validation/GPU_PROXY_C_FULL.mp4` completed under task `e74be9b7-ff43-464b-8b08-26cc15828033` (FFmpeg exit 0, ~12m04s with previously mixed NUT reused). FFprobe confirmed video 6854.033333s/854x480/yuv420p/205621 frames and AAC audio 6853.994688s; both 12-second tail decodes passed. The actual application CLI GPU run under task `c14c73e8-1565-4e43-a8fd-486f242bb3df` FAILED before CUDA video encode: its four-track lossless NUT mix ended at 3,204.052s vs 6,853.967s despite the previously successful full direct NUT mix and accepted CPU full run. Preserve accepted CPU outputs and standalone full-C GPU proxy; do not misattribute upstream variable mix EOF to CUDA or rerun blindly. Bounded-mix proof is complete: Source C 2700–3600s NUT lasted 899.999125s and 3600–4500s lasted 899.988125s; explicit-duration NUT concat stream-copy gave 1799.988125s and packet timestamps meet at 900.000000s. Implemented a long-form (>1800s) multi-track 900s-segment mix, validates every part, writes an exact-duration concat list and stream-copies lossless NUT before the unchanged limiter; cache v3 and double-NUT disk reserve. Focused tests 36 passed, 1 conditional skip, separately enabled CUDA integration 1 passed, Ruff and mypy src 74 PASS. Full Source C GPU application rerun `957f7676-5788-4c4a-8f3d-f84b1c050465` COMPLETED exit 0, with eight-part joined mono 16k float NUT `6853.929813s` against source `6853.967s`, fresh H.264 proxy video `6854.033333s` / AAC proxy audio `6853.994813s`, standalone analysis AAC `6853.994813s`, completed local-signals activity ending `6853967 ms` (13,708 bins, warnings 0), and 12s tail video/analysis-audio decodes exit 0. Functional full-C CUDA/audio EOF gate passed; total-pipeline speed comparison and subjective seam-quality review are separate. The 12m04s GPU-only derivative timing must not be presented as a matched end-to-end improvement against the earlier CPU path, which re-mixed audio. Do not overlap heavy transcodes or overwrite the accepted CPU proxy. The original provider-free C rerun task `261b7dca-e26e-4d07-86f4-34bedf679ac2` completed all local stages successfully; its fresh proxy video/audio and analysis audio have full duration. Full suite is now green: task `c6717d9c-bf81-487a-a5c7-731f5dabcd91` completed 324 passed, 1 CUDA-conditional skip; targeted offline fake-provider regressions 46/46 passed. Test-only synthetic verified timestamps removed the test-clock dependency; production pricing snapshot/freshness checks remain unchanged and fail-closed. Keep provider/upload gate closed; officially reverify live pricing and get separate authorization before paid inference. Remaining optional work is a controlled total-pipeline CPU/GPU benchmark and human listening review of segment joins; do not treat the GPU-only 12m04s result as matched end-to-end improvement.

## Source C signal-integrity note

The historical CPU `data-c` session has 12,872 audio-activity bins versus 13,708 expected; the fresh segmented-audio CUDA `data-c-gpu` session has all 13,708 half-second bins with exact start/end timestamps (zero gaps) and detects activity in 576 of the 836 historically absent bins. Use `data-c-gpu` for the next provider-free signal review; do not use the old incomplete CPU local-signals output for GT rescoring. A local seam-review pack is ready at `.t/c1-real-media-validation/seam-review/README.md`: seven unnormalized 6-second WAV clips centered on each 15-minute boundary and one source-mix reference at 5400s. The exact-boundary sample jump did not exceed other jumps in the surrounding 0.5-second window at any of the seven joins, but human listening is still required before asserting perceptually seamless audio. The old full-duration single-pass NUT has verified digital silence at 5399.5–5400.5s while the new AAC contains sound; never use that old NUT as a quality reference. No paid model calls are authorized, and the dated production pricing snapshot must be officially reverified before any future paid test.

## Current gate

Stop the paid window-length experiment loop. v20/v21 supplied enough evidence that shorter context helps fast-event detection, but the product North Star is broader: multi-game creator content discovery for audience growth.

No new provider call is currently authorized.

## Exact next objective

Implement the **C1 Hybrid Creator Triage + Story Verification** pivot from `docs/16_C1_HYBRID_CREATOR_TRIAGE_PLAN.md`. Preserve the existing media/extraction/cost/report substrate, but replace the monolithic semantic Scout center with provider-neutral proposals -> focused semantic judgment -> factual resolution verification -> story assembly/boundaries -> creator ranking. Do not start paid inference or C2 AI rough editing until this hybrid contract is stable provider-free.

### C1 implementation progress

Completed provider-free:

1. Added generic `DISCOVERY` to the controlled candidate taxonomy.
2. Added backward-compatible `moment_summary` (what happened) and `creator_reason` (why review it for short-form); legacy responses fall back to `reason`.
3. Kept creator/editorial score separate from detection/timing confidence and exposed an explicit `creator_score` in ranking artifacts.
4. Added creator-balanced `gemini-scout-window-v20-creator` as the first creator prompt; after A owner review failed standalone usefulness, provider-free revision `gemini-scout-window-v21-editorial-role` became the default contract with explicit standalone-vs-montage roles and real-resolution requirements.
5. Added hard prompt/editorial principles: `silence != dead air`, `no speech != boring`, and audio activity is evidence rather than an inclusion gate.
6. Preserved setup -> event -> payoff behavior and creator-facing semantics through canonicalization/reconciliation.
7. Converted report cards toward `Creator Candidate Pack`: `What happened`, `Why review this`, creator score, detection confidence, Open Clip, with technical lineage secondary.
8. Added focused creator tests plus offline archetype fixtures for quiet visual discovery, social/co-op payoff without gameplay payoff, tension/fail story, boring-zero-candidate behavior, and legacy compatibility.
9. Added a provider-free end-to-end C1 integration smoke test covering window payload -> canonicalization -> reconcile -> clip boundaries -> ranking -> extraction manifest -> Creator Candidate Pack report. Focused result: **4 passed in 0.56s**.
10. Generated an actual local five-family Creator Candidate Pack demo (`DISCOVERY`, `FUNNY`, `FAIL`, `REACTION`, `SKILL`) with playable synthetic MP4 fixtures and thumbnails; provider calls **0**.
11. Fresh complete local suite after the M6 local-windows safety fix is green: **280 passed in 226.41s (0:03:46)** via durable task `9c4044f7-41fb-4918-9bd1-6d26029b02c6`.
12. Synced `config.example.yaml` to the current creator contract; it now defaults to `gemini-scout-window-v21-editorial-role` so copied configs include editorial-role and resolution rules.
13. Added `docs/13_C1_REAL_MEDIA_VALIDATION_PLAN.md` with separate creator-review labels/metrics and three required archetypes.
14. Prepared V-C1-A fast-action cost preflight using the known cal-01 real source: 3 creator-prompt windows, **estimated aggregate reserve ฿2.514798**, provider calls **0**, uploads **0**, live authorization **false**.
15. Deep Research pivot plan is now canonical at `docs/16_C1_HYBRID_CREATOR_TRIAGE_PLAN.md`; architecture/pipeline/data-model/implementation docs are marked to preserve historical infrastructure while superseding the monolithic semantic Scout center.
16. Hybrid H1 implemented provider-free: `StoryState`, `ResolutionState`, evidence-backed `CandidateClaim`/`ClaimStatus`, and ranking gate that blocks unresolved/contradicted `STANDALONE_STORY` candidates from normal creator review.
17. Hybrid H2 implemented provider-free: provider-neutral `Proposal` / `ProposalArtifact` contracts plus deterministic local activity-signal adapter. Proposal records intentionally contain no creator score or editorial role.
18. Hybrid H3 implemented provider-free: proposal-centered context planner with bounded initial context, explicit `needs_more_context` expansion, source-edge clamping, and maximum-context exhaustion.
19. Hybrid H4/H5 contracts are implemented provider-free: sequential fake semantic judgments can request bounded context expansion, while a separate `CandidateVerification` / fake verifier contract alone may attach verified or contradicted terminal evidence.
20. Hybrid H6 is implemented provider-free: `StoryAssembly` records setup/event/payoff/reaction semantic boundaries and refuses standalone story assembly until the candidate is `COMPLETE` with `VERIFIED` or `NOT_APPLICABLE` resolution.
21. Added provider-free hybrid orchestration and persistence: proposal -> dynamic context -> semantic judgment -> factual verification -> story assembly -> creator-review eligibility -> clip-boundary derivation, with full/review maps persisted under `session/hybrid/`.
22. Added a separate creator-evaluation corpus for `KEEP` / `MAYBE` / rejection reasons, owner editorial-role corrections, desired boundary corrections, review time and `MISS_OBVIOUS`; these product labels do not mutate event GT.
23. Provider-free CLI/session workflow is integrated without replacing the historical flat-Scout path: `hybrid proposals`, `hybrid route`, `hybrid run-fixture`, `hybrid review-template`, and `hybrid review-summary` persist/consume hybrid artifacts deterministically.
24. H2 evidence enrichment/routing is implemented: source-bound `ManualProposalMarkerSet`, source-bound `TranscriptFixture` / `ASR_UTTERANCE`, and source-bound `VisualEvidenceFixture` / `VISUAL_STATE_CHANGE` / `OCR_STATE_CHANGE` adapter contracts feed compatible-neighborhood clustering, `ProposalSummary`, and explicit routing decisions that preserve deferred weak evidence for audit. Visual/OCR fixtures are factual ingestion boundaries only; do not replay benchmark GT as detector output.
25. Creator review round-trips deterministically from review map -> editable worksheet -> separate creator-evaluation corpus/summary with KEEP/MAYBE/rejection, role/boundary corrections, review time and `MISS_OBVIOUS` metrics; the historical Spike replay is a mandatory provider-free semantic regression.
26. Transcript/routing full-suite validation is green: **309 passed in 177.92s (0:02:57)** via task `3c26aa87-0ab2-4e11-8f3d-30f7a2e62ad5`. Latest focused Hybrid/CLI/M6 regression at that checkpoint is **47 passed in 56.37s**; Ruff is green and `mypy src` is green across **72 source files**.
27. H3 media materialization is implemented provider-free: routed proposals now become deterministic initial `hybrid/contexts/<context_id>/analysis_context.mp4` clips cut only from the committed analysis proxy, with context provenance/hash/materialization identity, cache reuse, upload-path validation, and a local `hybrid contexts` CLI. RAW source is never accepted as context-provider input.
28. Routed context-media validation is green: focused Hybrid/CLI/M6 **49 passed in 133.74s**, Ruff is green across `src` + `tests`, mypy is green across **73 source files**, and the fresh full suite is **311 passed in 123.02s (0:02:03)** via task `47b33ae3-f340-4223-8301-95f151253094`.
29. Hybrid provider-boundary preflight is implemented provider-free: `hybrid provider-preflight <session>` validates only committed routed `analysis_context.mp4` media, separates `SEMANTIC_JUDGE` from `RESOLUTION_VERIFIER`, persists prompt/schema hashes + request fingerprint + cache key, counts logical calls/media minutes, quotes exact reserve without ledger reservation, fixes one generation attempt with zero automatic retry, excludes dynamic-expansion calls until a separate re-preflight, and leaves live authorization/uploads/provider calls/reservations at zero. RAW source remains ineligible provider input.
30. Provider schema drift is now fail-closed against the canonical hybrid contracts: provider projections reuse canonical version/enums and assert field-set parity for `SemanticJudgment`, `CandidateVerification`, nested `CandidateClaim`, and `Evidence`. Validation is green: focused **22 passed in 0.90s**, Ruff green, `mypy src` green across **74 source files**, and full suite **314 passed in 145.51s (0:02:25)** via task `8afb5210-a28b-4efc-9b4e-6b27eddf743d`.

### Immediate provider-free work

**C1 EXECUTION RESUMED UNDER RESTRICTED FILE-SCOPE MODE.** Continue normal VDO source/test/media work, but do **not** mass-rename, move a repo/worktree, move a large folder, run recursive cleanup, or create a new workspace structure until the Supervisor Cleanup/Scope fix is ready. Keep the existing canonical paths stable. The pre-cleanup preservation record remains in `docs/14_PRE_REORG_CHECKPOINT_2026-09-07.md`; the scoped cleanup record is in `docs/15_VDO_FOLDER_CLEANUP_2026-09-07.md`.

Current media-correctness gate (2026-09-11): long-VOD local-signal tail truncation is fixed provider-free and its prior regression gates are green (**72/72 focused, 320/320 full, Ruff PASS, mypy src PASS**). V-C1-C failure is now isolated to FFmpeg interactive-stdin susceptibility: the exact production audio command without `-nostdin` ended gracefully at `00:45:33.013` despite source packets continuing, while the same `amix + alimiter + AAC/M4A` graph with `-nostdin` completed the locked 2800-second diagnostic at `00:46:40.000` and ffprobe measured `2800.021333` seconds. All seven automated FFmpeg builders now include `-nostdin`; silence padding remains rejected because the source audio is genuinely present later. Post-hardening Ruff and `mypy src` are green and full-suite task `019dee57-3878-411a-bf22-0ff71ff46a96` passed **320/320 in 155.86s**. Fresh C provider-free rerun task `9451bc77-f79f-4bf8-bdb1-28e4369bcb13` / proxy attempt `01dc872a050b4a63bf0f57c6f711f4f4` failed again at analysis-audio duration validation with `2,556,523 vs 6,853,967 ms`, despite `-nostdin`. Treat `-nostdin` as execution hardening, not the complete root cause. The full production graph diagnostic `f7b41477-33c3-4175-92bd-e131ca2d9b5f` reproduced a silent graceful cutoff at `00:43:55.968`; removing only `alimiter` completed the full source at `01:54:13.930688`, with ffprobe `6853.994688s`. Treat `alimiter` as the isolated failure boundary. The `alimiter=limit=0.95:latency=1` fix-candidate task `ea4fb7d4-1390-4302-b98d-a439f34df752` also cut off at `00:43:33.930688`, exit 0, no stderr, so do not patch it. The single-track limiter isolate task `34b7077d-2ab3-4ea7-9a5c-b023858b29fb` is full-duration (`6853.994688s`), so the failure is specifically the `amix(normalize=0) -> alimiter` interaction rather than `alimiter` alone. The `asoftclip=type=hard:threshold=0.95` replacement diagnostic task `e8971389-9338-4aaa-9574-11fe8e8beefe` also truncated (`00:38:21.802688`, ffprobe `2301.866688s`), so do not patch it. The pass-through control task `6212ebf2-5127-46a4-9554-18a7a72fd057` is full-duration (`6853.994688s`), proving generic post-`amix` chaining is healthy and refining the failure to the amplitude/dynamics processors tested on the mixed stream. The raw-mix measurement is complete via sequential <=20-minute chunks because full-source `astats` itself early-EOFs. Worst pre-encode peak is `1.754737` / `+5.198178 dBFS` (4800-6000s), with NaN=0 and Inf=0 in every chunk, so simply removing anti-clipping is not acceptable. The C-only static attenuation to 0.95 would be about `0.541392` (`-5.32977 dB`), but do not hardcode a source-specific factor. The `amix -> asetpts=N/SR/TB -> alimiter` candidate is rejected at ffprobe `2300.224000s`. Treat `amix normalize=1` as rejected at ffprobe `3490.944000s`. File-boundary two-pass is the first anti-clipping strategy proven full-duration: full `amix normalize=0` artifact -> second-process `alimiter=limit=0.95` probes at `6853.994688s`; decoded full-file scan is `Max_level=1.052555` / `+0.444895 dBFS`, NaN=0, Inf=0. A `limit=0.90` rerun also stays full-duration but decoded peak does not scale downward because local FFmpeg confirms `alimiter level` auto-level defaults to true. The second-pass `alimiter=limit=0.95:level=0` candidate is full-duration (`6853.994688s`) and improves decoded peak to `1.021416` / `+0.184052 dBFS`, but still leaves slight AAC overshoot. The second-pass `alimiter=limit=0.90:level=0` candidate is accepted: task `51ad9bb5-8581-4148-b423-864aeb9480d7` is full-duration at `6853.994688s`, with decoded `Max_level=0.979226` / `-0.182345 dBFS`, NaN=0 and Inf=0. Treat exact WAV float proof task `a978ede7-31d1-48c6-997e-82a2654686c8` as rejected: `amix normalize=0` -> `pcm_f32le` mono 16 kHz WAV exited 0 at only `2975.914688s` (~49:35). Do not patch that topology. Treat container isolate task `7d4bc564-1971-4a6c-b443-b8f092c26e89` as accepted: `pcm_f32le` mono 16 kHz NUT probes at `6853.929625s`, within ~37 ms of source. Exact NUT pass-2 task `3fb7a4c0-b381-4bec-9bb1-8db1e2040915` is accepted: full AAC `6853.994688s`, decoded peak `0.972900` / `-0.073253 dBFS`, NaN=0, Inf=0. Production fix is patched but uncommitted: multi-track NUT float -> separate proxy/analysis limiter `0.90:level=0`, short extraction unchanged, multi-track disk preflight and proxy cache v2. Focused 26/26, Ruff, and mypy src 74 pass. Full suite task `39425b74-6463-4443-9cb6-2b46a3766869` is NOT green: first failure is stale Gemini pricing fixture CostGateError; inspect remaining failure categories without bypassing cost gates. Continue provider-free Source C task `261b7dca-e26e-4d07-86f4-34bedf679ac2` (`--stop-after local-signals --scout-backend fake`), then ffprobe both proxy audio stream and analysis audio against source duration and verify local-signals tail coverage. If C passes, review diff and classify full-suite blocker before commit. Do not add silence padding or move to semantic/provider work until full-source proxy + analysis-audio + local-signals duration/tail coverage is proven. Provider authorization remains false.

1. Preserve the live A baseline exactly: 3 calls, settled cost **฿0.996318**, 6 extracted candidates, owner standalone usefulness **0/6**. Treat it as the regression baseline for the architecture pivot.
2. H1 is implemented: preserve the new story/resolution/claim invariants and extend tests rather than weakening the standalone-review gate for convenience.
3. Keep the Spike case as a mandatory provider-free semantic regression: plant/objective state change is allowed, but `ROUND_WON` / `CLUTCH_WIN` cannot become verified without terminal evidence.
4. H2 is implemented as an interface proof. Next enrich proposal sources (ASR/visual/OCR/game adapters) only after the core orchestration is coherent; do not promote loudness/activity into creator truth.
5. Keep v21 flat Scout artifacts/configs as historical baseline only. Do not spend on a v22/v23 monolithic prompt loop as the main fix.
6. B and C remain fully prepared but paused: B **17 windows / ฿15.224148 preflight**, C **26 windows / ฿23.218504 preflight**, provider calls 0 / uploads 0 for both. These cost artifacts are historical planning evidence until the hybrid architecture reaches a new provider boundary.
7. CLI/session integration and deterministic owner-review round-trip are implemented. Preserve those contracts and use them for local learning rather than rebuilding another parallel review path.
8. H2.1/H2.2/H2.3 evidence enrichment contracts are implemented: source-bound manual markers, transcript/ASR fixtures, and bounded visual/OCR evidence fixtures feed compatible-neighborhood clustering, persisted `ProposalSummary`, and auditable proposal routing. Transcript and visual/OCR observations are factual retrieval evidence only; do not assume speech/visual change implies creator value, and do not use benchmark GT as detector output.
9. Historical A proposal-density evidence is **860 weak audio anchors -> 84 clustered neighborhoods over 600.886s (~503.26 neighborhoods/source-hour)**. Coverage-only routing diagnostics select ~119.8/hour at 30s, ~59.9/hour at 60s, or ~30.0/hour at 120s. These are experiment points only; do not lock a universal interval.
10. Historical B and C local-signal artifacts produced 0 neighborhoods apiece; fresh C data-c-gpu now produces **896 AUDIO_ACTIVITY-only neighborhoods (~470.618/hour)** from 10,761 weak anchors, with no explicit event hypotheses or multi-source support. An experimental 120-second routing pass selected **58 sampled weak neighborhoods (~30.464/hour)** and deferred 838, spanning all eight 15-minute source bins. Treat the old-versus-new C contrast as a data-integrity and audio-mix change, not proof that loudness detects creator value or that missing bins alone caused the original zero; do not lock a universal threshold.
11. Preserve the historical Spike replay invariant: a semantic `ROUND_WON` / `CLUTCH_WIN` hypothesis cannot enter standalone review when the independent verifier lacks terminal evidence.
12. Routed proposal-centered context media is now materialized locally from the committed analysis proxy with deterministic provenance/cache identity and RAW-source rejection. Preserve this as the only future semantic-provider media boundary; do not fall back to old full-window or RAW upload semantics.
13. The hybrid provider-boundary/preflight contract is now implemented provider-free. Preserve its hard invariants: only committed routed `analysis_context.mp4` media may be planned, semantic claims remain provisional, verifier truth requires timestamped evidence, one attempt/zero auto-retry is fixed, expansion requires separate re-preflight, RAW source is forbidden, and normal chat continuation never authorizes upload/generation.
14. H2.3 visual/OCR fixture ingestion is implemented and provider-free (final full suite 327 passed/1 CUDA-conditional skip after metadata-provenance hardening). The first real source-bound non-audio slice is now measured on V-C1-B: 52 diagnostic visual observations produce 43 multi-source-supported neighborhoods while preserving complete 120-second temporal coverage. All 55 routed initial contexts are now materialized locally and a deterministic 36-item pre-review cohort is locked. **Next real learning step:** review that cohort and collect separate creator-usefulness / obvious-miss evidence so supported routing can be judged against actual review burden. Do not backfill detector evidence from benchmark GT or claim visual routing improves creator quality before that review.
15. Keep existing ingest/proxy/source identity/original-source extraction/reconcile/cost ledger/report infrastructure unless a concrete regression proves it must change.
16. Keep one coordinator/single writer and preserve current benchmark/config paths. Do not optimize GPU/media behavior mid-semantic validation.
17. Run focused + full regression, final handoff/diff checks, and checkpoint each coherent provider-free slice before any paid inference.

Do not lock 90-second, 120-second or 300-second windows as a universal semantic unit. Window duration remains transport/coarse-context infrastructure until the hybrid pipeline has real creator evidence.

## Creator-beta acceptance

Do not require benchmark perfection. Creator Candidate Pack Beta is useful when:

- a long VOD can complete end-to-end reliably;
- the pack is much faster to review than the full source;
- boring burden is tolerable;
- obvious creator-worthy moments are not routinely missed;
- setup/payoff context is good enough for an editing pass;
- behavior remains useful across different game styles;
- cost/source-hour is reasonable.

## AI rough editor (C2) — immediately after C1 is usable

Do not build a fully autonomous TikTok editor first. Add an owner-selected-candidate rough-editor path that produces non-destructive draft variants.

First target features:

- remove/tighten dead air;
- suggest/select a hook beat;
- 9:16 safe reframing;
- draft subtitles;
- loudness cleanup;
- restrained punch-ins/zooms;
- optional on-screen hook/caption suggestions;
- preserve original extracted candidate beside all drafts.

C2 must be conservative by default: clean/minimal first, preserve setup/event/payoff and reaction tails, avoid unjustified effects, and keep every draft reversible/traceable to source timestamps. If edit confidence is low, produce a small set of variants (for example Clean / Balanced / Story-Hook) rather than forcing one cut. Human approval is required before publish-ready output.

Prefer model-generated edit decisions + deterministic FFmpeg transforms before fully generative video editing.

## Later creator feedback loop (C3)

Once the channel has real posts, collect candidate selected/rejected, edit choices, final duration, and post-performance signals. Use them to tune creator-specific ranking/editing. Keep these labels separate from event-detection benchmark truth.

Canonical roadmap:

`docs/10_CREATOR_GROWTH_AND_AI_EDITING_PLAN.md`

## Hard rules

- No new provider call without fresh explicit authorization.
- No automatic provider generation retry.
- Never upload raw source.
- Do not overwrite GT history.
- Do not treat FPS kill benchmarks as the product definition.
- Do not clean/reset/stash/drop unrelated dirty work.
